name: CodeQL
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '0 0 * * 0'
jobs:
  analyze:
    uses: github/codeql-action/analyze@v3
    with:
      category: '/language:javascript'
